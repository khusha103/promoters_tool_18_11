import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DailyVmdchecklistFeedphotoPage } from './daily-vmdchecklist-feedphoto.page';

describe('DailyVmdchecklistFeedphotoPage', () => {
  let component: DailyVmdchecklistFeedphotoPage;
  let fixture: ComponentFixture<DailyVmdchecklistFeedphotoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DailyVmdchecklistFeedphotoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
