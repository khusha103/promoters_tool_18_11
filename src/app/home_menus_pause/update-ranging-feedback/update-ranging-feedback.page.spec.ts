import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateRangingFeedbackPage } from './update-ranging-feedback.page';

describe('UpdateRangingFeedbackPage', () => {
  let component: UpdateRangingFeedbackPage;
  let fixture: ComponentFixture<UpdateRangingFeedbackPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateRangingFeedbackPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
